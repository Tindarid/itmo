select StudentName, AvgMark from Students left join (select StudentId, cast(sum(Mark) as float) / cast(count(*) as float) as AvgMark from Marks group by StudentId) R1 on Students.StudentId = R1.StudentId
